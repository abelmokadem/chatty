{
  "name": "styles",
  "version": "0.1.0",
  "dependencies": {
    "@frctl/fractal": "^1.0.11"
  },
  "devDependencies": {
    "concurrently": "^3.1.0",
    "node-sass": "^4.1.1"
  },
  "scripts": {
    "start": "concurrently  --kill-others \"fractal start\" \"npm run build -- --watch\"",
    "build": "./bin/build.sh"
  }
}
